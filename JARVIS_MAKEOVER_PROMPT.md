# JARVIS Dashboard — Makeover Prompt
### (paste this into Claude Design, and attach the file `jarvis-dashboard-CURRENT.html`)

---

I have a **live, fully-built** analytics dashboard — "Funnelmaxxing Pro," a sales-funnel command center for a crypto-trading-education business. It already works and already displays 100+ real metrics across 16 sections.

**Attached is `jarvis-dashboard-CURRENT.html` — a single self-contained file that IS the current page**, with every real number and all 16 sections baked in. Open it in a browser to see exactly what exists today. I want a **cinematic visual makeover** — a Stark / JARVIS HUD look — **without changing what data it shows or how much of it shows.** Re-skin it; do not redesign what's on it.

## ⛔ THE ONE HARD RULE (non-negotiable)

This is a **data-maximalist** dashboard. Restyle every number, table row, chart, funnel node, and gauge in that HTML — do **NOT** remove, merge, summarize, or "simplify" any of it. The file contains ~13,000 individual numbers; they all stay. **If your makeover shows fewer numbers than the HTML, it has failed and will be rejected.** Density IS the aesthetic. Reference: a **Bloomberg terminal crossed with an Iron Man HUD** — dense walls of live data that look gorgeous. Make the density beautiful; never thin it out.

## What I want back

1. **A cohesive visual style system** — evolved palette (keep the lime green `#BFFA46` as the signature/money color), type scale, panel/card/table treatments, glow & shadow recipes, the **JARVIS orb** styling, the **living-funnel** art direction, **HUD gauge faces**, and a motion language (what animates, when, how).
2. **Restyled mockups** of each section in the attached HTML — desktop-first, annotated. Same content, new skin.
3. **Exact tokens** — hex colors, px sizes, font weights, letter-spacing, radii, shadow/glow recipes — so an engineer can build it faithfully.
4. **Any assets** — SVG / Lottie, an icon set for funnel events, the orb, gauge faces. Vector preferred.

## Aesthetic direction

Dark, premium, *alive*. JARVIS / Stark HUD; Bloomberg-terminal-meets-spaceship; high-end dark fintech. Glowing edges, subtle scanlines/grain, sense of depth. Must stay fast and legible — density without clutter.

## The 16 sections to restyle (all present in the HTML)

JARVIS daily-brief panel (orb + brief + "brief me out loud" voice + "my read" feedback) · Morning Report · Action Center (ranked $-impact directives) · Scoreboard · the Living Funnel board · Daily lead-volume chart · Revenue sliced every way + product table · Free-course funnel · Broker order-bump (Hydra/BloFin) · VSL detail + qualify split · Quantum booking sub-funnel · Wolf path ($997/$297) · Sales-team table (closer + setters) · Email nurture + broadcasts table · Traffic sources · Growth trends · Live feed.

## Constraints

- It will be **built in React / Next.js by an engineer (Claude Code)** — hand back specs, mockups, tokens, and assets; **not** code.
- All data is **real and fixed** — do not invent metrics that aren't in the HTML.
- A deeper data contract + a mandatory per-section metric inventory (§5.7) and completeness checklist (§7.2) are in the companion `FUNNELMAXXING_JARVIS_DESIGN_BRIEF.md` if you want them — but the attached HTML is the source of truth for what must survive.

**Goal: make it look like the future without losing a single number.**
